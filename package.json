{
  "name": "slashauth",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "app/*",
    "database/*"
  ],
  "scripts": {
    "dev": "pnpm --stream -r dev",
    "build": "pnpm --stream -r build",
    "start": "pnpm --stream -r start"
  },
  "devDependencies": {
    "prettier": "latest"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {},
  "packageManager": "pnpm@7.9.5"
}
